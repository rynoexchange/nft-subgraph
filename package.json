{
  "name": "nft",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --product hosted-service",
    "deploy": "graph deploy --product hosted-service --node https://api.thegraph.com/deploy/ rynoexchange/nft",
    "create-local": "graph create --node http://localhost:8020/ rynoexchange/nft",
    "remove-local": "graph remove --node http://localhost:8020/ rynoexchange/nft",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 rynoexchange/nft"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.22.3",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
